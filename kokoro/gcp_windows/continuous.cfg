# Format: //devtools/kokoro/config/proto/build.proto

build_file: "cmake_src/kokoro/kokoro_build.cmd"

action {
  define_artifacts {
    regex: "dest/*.tar.bz2"
    strip_prefix: "dest/"
    fail_if_no_artifacts: true
  }
}
